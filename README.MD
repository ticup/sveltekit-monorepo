# proof of concept for a monorepo with typescript projects and sveltekit

```bash
> npm install
> npm install -w apps/sveltekit
> cd packages/core
> npm run build
> cd ../../apps/sveltekit
> npm run dev
```
